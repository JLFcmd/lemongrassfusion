{
    "name": "lemongrass-fusion-web",
    "version": "1.0.0",
    "description": "Restaurant website with AI Chatbot",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@google/generative-ai": "^0.1.3",
        "dotenv": "^16.3.1"
    }
}
